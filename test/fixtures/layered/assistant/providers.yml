anthropic:
  name: Anthropic
  protocol: anthropic
  enabled: true
  secret: sk-ant-test
  position: 1
  owner: one (User)

openai:
  name: OpenAI
  protocol: openai
  enabled: true
  secret: sk-oai-test
  position: 2
  owner: one (User)

disabled:
  name: Disabled Provider
  protocol: openai
  enabled: false
  secret: sk-disabled
  position: 3
  owner: one (User)
